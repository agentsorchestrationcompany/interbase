diff --git a/index-d07rkqtc.js b/index-d07rkqtc.js
index 93f8dcb8d..3c2ff3e91 100644
--- a/index-d07rkqtc.js
+++ b/index-d07rkqtc.js
@@ -3143,7 +3143,8 @@ class ASCIIFontRenderable extends FrameBufferRenderable {
     return this.selectionHelper.shouldStartSelection(localX, localY, this.width, this.height);
   }
   onSelectionChanged(selection) {
-    const localSelection = convertGlobalToLocalSelection(selection, this.x, this.y);
+    const mode = selection?.mode ?? "character";
+    const localSelection = adjustAsciiFontLocalSelection(this, convertGlobalToLocalSelection(selection, this.x, this.y), mode);
     this.lastLocalSelection = localSelection;
     const changed = this.selectionHelper.onLocalSelectionChanged(localSelection, this.width, this.height);
     if (changed) {
@@ -3178,12 +3179,14 @@ class ASCIIFontRenderable extends FrameBufferRenderable {
       font: this._font
     });
     const selection = this.selectionHelper.getSelection();
-    if (selection && (this._selectionBg || this._selectionFg)) {
-      this.renderSelectionHighlight(selection);
+    const selectionBg = selectionBgForRenderable(this);
+    const selectionFg = selectionFgForRenderable(this);
+    if (selection && (selectionBg || selectionFg)) {
+      this.renderSelectionHighlight(selection, selectionBg, selectionFg);
     }
   }
-  renderSelectionHighlight(selection) {
-    if (!this._selectionBg && !this._selectionFg)
+  renderSelectionHighlight(selection, selectionBg, selectionFg) {
+    if (!selectionBg && !selectionFg)
       return;
     const selectedText = this._text.slice(selection.start, selection.end);
     if (!selectedText)
@@ -3191,16 +3194,16 @@ class ASCIIFontRenderable extends FrameBufferRenderable {
     const positions = getCharacterPositions(this._text, this._font);
     const startX = positions[selection.start] || 0;
     const endX = selection.end < positions.length ? positions[selection.end] : measureText({ text: this._text, font: this._font }).width;
-    if (this._selectionBg) {
-      this.frameBuffer.fillRect(startX, 0, endX - startX, this.height, parseColor(this._selectionBg));
+    if (selectionBg) {
+      this.frameBuffer.fillRect(startX, 0, endX - startX, this.height, parseColor(selectionBg));
     }
-    if (this._selectionFg || this._selectionBg) {
+    if (selectionFg || selectionBg) {
       renderFontToFrameBuffer(this.frameBuffer, {
         text: selectedText,
         x: startX,
         y: 0,
-        color: this._selectionFg ? this._selectionFg : this._color,
-        backgroundColor: this._selectionBg ? this._selectionBg : this._backgroundColor,
+        color: selectionFg ? selectionFg : this._color,
+        backgroundColor: selectionBg ? selectionBg : this._backgroundColor,
         font: this._font
       });
     }
@@ -3505,6 +3508,82 @@ class GutterRenderable extends Renderable {
 function darkenColor(color) {
   return RGBA.fromValues(color.r * 0.8, color.g * 0.8, color.b * 0.8, color.a);
 }
+function selectionBgForRenderable(renderable) {
+  return renderable._selectionBg ?? renderable._ctx?.selectionBg;
+}
+function selectionFgForRenderable(renderable) {
+  return renderable._selectionFg ?? renderable._ctx?.selectionFg;
+}
+function selectionClamp(value, min, max) {
+  return Math.max(min, Math.min(max, value));
+}
+function selectionWordClass(char) {
+  if (!char)
+    return "space";
+  if (/\s/u.test(char))
+    return "space";
+  return "word";
+}
+function selectionWordBounds(text, column) {
+  if (!text.length) {
+    return { start: 0, end: 0 };
+  }
+  const safeColumn = selectionClamp(column, 0, text.length);
+  const index = safeColumn === text.length && selectionWordClass(text[text.length - 1]) !== "space" ? Math.max(0, text.length - 1) : safeColumn;
+  const kind = selectionWordClass(text[index]);
+  let start = index;
+  let end = Math.min(text.length, index + 1);
+  while (start > 0 && selectionWordClass(text[start - 1]) === kind) {
+    start--;
+  }
+  while (end < text.length && selectionWordClass(text[end]) === kind) {
+    end++;
+  }
+  return { start, end };
+}
+function selectionParagraphBounds(text, column) {
+  if (!text.length) {
+    return { start: 0, end: 0 };
+  }
+  const safeColumn = selectionClamp(column, 0, text.length);
+  const index = safeColumn === text.length ? Math.max(0, text.length - 1) : safeColumn;
+  let start = index;
+  let end = index;
+  while (start > 0 && text[start - 1] !== `
+`) {
+    start--;
+  }
+  while (end < text.length && text[end] !== `
+`) {
+    end++;
+  }
+  return { start, end };
+}
+function selectionBounds(text, column, mode) {
+  return mode === "paragraph" ? selectionParagraphBounds(text, column) : selectionWordBounds(text, column);
+}
+function textOffsetToAsciiX(text, font, offset) {
+  const positions = getCharacterPositions(text, font);
+  return offset < positions.length ? positions[offset] : measureText({ text, font }).width;
+}
+function adjustAsciiFontLocalSelection(renderable, localSelection, mode) {
+  if (!localSelection?.isActive || mode === "character") {
+    return localSelection;
+  }
+  const expand = (x, edge) => {
+    const charIndex = x < 0 ? -1 : coordinateToCharacterIndex(x, renderable._text, renderable._font);
+    if (charIndex < 0) {
+      return x;
+    }
+    const bounds = selectionBounds(renderable._text, charIndex, mode);
+    return textOffsetToAsciiX(renderable._text, renderable._font, edge === "start" ? bounds.start : bounds.end);
+  };
+  return {
+    ...localSelection,
+    anchorX: expand(localSelection.anchorX, localSelection.anchorX <= localSelection.focusX ? "start" : "end"),
+    focusX: expand(localSelection.focusX, localSelection.anchorX <= localSelection.focusX ? "end" : "start")
+  };
+}

 class LineNumberRenderable extends Renderable {
   gutter = null;
@@ -5581,6 +5660,94 @@ class InputRenderable extends TextareaRenderable {
 }
 // src/renderables/TextTable.ts
 var MEASURE_HEIGHT = 1e4;
+function tableCellMetrics(table, rowIdx, colIdx) {
+  return {
+    left: (table._layout.columnOffsets[colIdx] ?? 0) + 1 + table._cellPadding,
+    top: (table._layout.rowOffsets[rowIdx] ?? 0) + 1 + table._cellPadding
+  };
+}
+function selectionLineAt(text, line) {
+  let start = 0;
+  for (let idx = 0;idx < line; idx++) {
+    const next = text.indexOf(`\n`, start);
+    if (next === -1)
+      return "";
+    start = next + 1;
+  }
+  const end = text.indexOf(`\n`, start);
+  return end === -1 ? text.slice(start) : text.slice(start, end);
+}
+function tableCellLineInfo(cell, localY) {
+  const info = cell.textBufferView.logicalLineInfo;
+  const starts = info?.lineStartCols ?? [];
+  const visualLineCount = Math.max(1, starts.length);
+  const visualLine = selectionClamp(Math.floor(localY), 0, visualLineCount - 1);
+  const sourceLine = info?.lineSources?.[visualLine] ?? visualLine;
+  const startCol = info?.lineStartCols?.[visualLine] ?? 0;
+  const text = selectionLineAt(cell.textBuffer.getPlainText(), sourceLine);
+  return { visualLine, sourceLine, startCol, text };
+}
+function tableCellParagraphEdgePoint(cell, line, edge) {
+  const info = cell.textBufferView.logicalLineInfo;
+  const sources = info?.lineSources ?? [];
+  let firstVisualLine = line.visualLine;
+  let lastVisualLine = line.visualLine;
+  for (let idx = line.visualLine;idx >= 0; idx--) {
+    if ((sources[idx] ?? idx) !== line.sourceLine) {
+      break;
+    }
+    firstVisualLine = idx;
+  }
+  for (let idx = line.visualLine;idx < sources.length; idx++) {
+    if ((sources[idx] ?? idx) !== line.sourceLine) {
+      break;
+    }
+    lastVisualLine = idx;
+  }
+  const visualLine = edge === "start" ? firstVisualLine : lastVisualLine;
+  const startCol = info?.lineStartCols?.[visualLine] ?? 0;
+  return {
+    x: edge === "start" ? 0 : Math.max(0, line.text.length - startCol),
+    y: visualLine
+  };
+}
+function adjustTableSelectionPoint(table, localX, localY, edge, mode) {
+  const position = table.getCellAtLocalPosition(localX, localY);
+  if (!position)
+    return { x: localX, y: localY };
+  const cell = table._cells[position.rowIdx]?.[position.colIdx];
+  if (!cell)
+    return { x: localX, y: localY };
+  const metrics = tableCellMetrics(table, position.rowIdx, position.colIdx);
+  const line = tableCellLineInfo(cell, localY - metrics.top);
+  const column = selectionClamp(line.startCol + Math.floor(localX - metrics.left), 0, line.text.length);
+  if (mode === "paragraph") {
+    const point = tableCellParagraphEdgePoint(cell, line, edge);
+    return {
+      x: metrics.left + point.x,
+      y: metrics.top + point.y
+    };
+  }
+  const bounds = selectionBounds(line.text, column, mode);
+  return {
+    x: metrics.left + (edge === "start" ? bounds.start : bounds.end) - line.startCol,
+    y: metrics.top + line.visualLine
+  };
+}
+function adjustTableLocalSelection(table, localSelection, mode) {
+  if (!localSelection?.isActive || mode === "character") {
+    return localSelection;
+  }
+  const anchor = adjustTableSelectionPoint(table, localSelection.anchorX, localSelection.anchorY, localSelection.anchorY < localSelection.focusY || localSelection.anchorY === localSelection.focusY && localSelection.anchorX <= localSelection.focusX ? "start" : "end", mode);
+  const focus = adjustTableSelectionPoint(table, localSelection.focusX, localSelection.focusY, localSelection.anchorY < localSelection.focusY || localSelection.anchorY === localSelection.focusY && localSelection.anchorX <= localSelection.focusX ? "end" : "start", mode);
+  return {
+    ...localSelection,
+    anchorX: anchor.x,
+    anchorY: anchor.y,
+    focusX: focus.x,
+    focusY: focus.y
+  };
+}

 class TextTableRenderable extends Renderable {
   _content;
@@ -5770,6 +5937,32 @@ class TextTableRenderable extends Renderable {
     this._borderColor = next;
     this.invalidateRasterOnly();
   }
+  get selectionBg() {
+    return this._selectionBg;
+  }
+  set selectionBg(value) {
+    const next = value ? parseColor(value) : undefined;
+    if (this._selectionBg?.r === next?.r && this._selectionBg?.g === next?.g && this._selectionBg?.b === next?.b && this._selectionBg?.a === next?.a)
+      return;
+    this._selectionBg = next;
+    if (this._lastLocalSelection?.isActive) {
+      this.applySelectionToCells(this._lastLocalSelection, true);
+    }
+    this.invalidateRasterOnly();
+  }
+  get selectionFg() {
+    return this._selectionFg;
+  }
+  set selectionFg(value) {
+    const next = value ? parseColor(value) : undefined;
+    if (this._selectionFg?.r === next?.r && this._selectionFg?.g === next?.g && this._selectionFg?.b === next?.b && this._selectionFg?.a === next?.a)
+      return;
+    this._selectionFg = next;
+    if (this._lastLocalSelection?.isActive) {
+      this.applySelectionToCells(this._lastLocalSelection, true);
+    }
+    this.invalidateRasterOnly();
+  }
   shouldStartSelection(x, y) {
     if (!this.selectable)
       return false;
@@ -5781,7 +5974,8 @@ class TextTableRenderable extends Renderable {
   onSelectionChanged(selection) {
     this.ensureLayoutReady();
     const previousLocalSelection = this._lastLocalSelection;
-    const localSelection = convertGlobalToLocalSelection(selection, this.x, this.y);
+    const mode = selection?.mode ?? "character";
+    const localSelection = adjustTableLocalSelection(this, convertGlobalToLocalSelection(selection, this.x, this.y), mode);
     this._lastLocalSelection = localSelection;
     const dirtyRows = this.getDirtySelectionRowRange(previousLocalSelection, localSelection);
     if (!localSelection?.isActive) {
@@ -6425,6 +6619,8 @@ class TextTableRenderable extends Renderable {
     const modeChanged = this._lastSelectionMode !== selection.mode;
     this._lastSelectionMode = selection.mode;
     const lockToAnchorColumn = selection.mode === "column-locked" && selection.anchorColumn !== null;
+    const selectionBg = selectionBgForRenderable(this);
+    const selectionFg = selectionFgForRenderable(this);
     for (let rowIdx = 0;rowIdx < this._rowCount; rowIdx++) {
       if (rowIdx < firstRow || rowIdx > lastRow) {
         this.resetRowSelection(rowIdx);
@@ -8521,7 +8717,9 @@ class MarkdownRenderable extends Renderable {
       showBorders: borders,
       borderStyle: this._tableOptions?.borderStyle ?? "single",
       borderColor: this._tableOptions?.borderColor ?? this.getStyle("conceal")?.fg ?? "#888888",
-      selectable: this._tableOptions?.selectable ?? true
+      selectable: this._tableOptions?.selectable ?? true,
+      selectionBg: this._tableOptions?.selectionBg,
+      selectionFg: this._tableOptions?.selectionFg
     };
   }
   applyTableRenderableOptions(tableRenderable, options) {
@@ -8536,6 +8734,8 @@ class MarkdownRenderable extends Renderable {
     tableRenderable.borderStyle = options.borderStyle;
     tableRenderable.borderColor = options.borderColor;
     tableRenderable.selectable = options.selectable;
+    tableRenderable.selectionBg = options.selectionBg;
+    tableRenderable.selectionFg = options.selectionFg;
   }
   applyTableOptionsToBlocks() {
     const options = this.resolveTableRenderableOptions();
@@ -8567,7 +8767,9 @@ class MarkdownRenderable extends Renderable {
       showBorders: options.showBorders,
       borderStyle: options.borderStyle,
       borderColor: options.borderColor,
-      selectable: options.selectable
+      selectable: options.selectable,
+      selectionBg: options.selectionBg,
+      selectionFg: options.selectionFg
     });
   }
   createTableBlock(table, id, marginBottom = 0, previousCache, forceRegenerate = false) {
@@ -9986,6 +10188,8 @@ class ScrollBoxRenderable extends BoxRenderable {
     if (event.type === "drag" && event.isDragging) {
       this.updateAutoScroll(event.x, event.y);
     } else if (event.type === "up") {
+      this.scrollAccel.reset();
+      this.resetScrollAccumulators();
       this.stopAutoScroll();
     }
   }
diff --git a/index-jv9g79dk.js b/index-jv9g79dk.js
index 378e8a297..51bab4dda 100644
--- a/index-jv9g79dk.js
+++ b/index-jv9g79dk.js
@@ -6521,6 +6521,7 @@ class MouseParser {
         type = "move";
       }
     } else if (isScroll && pressRelease === "M") {
+      this.mouseButtonsPressed.clear();
       type = "scroll";
       scrollInfo = {
         direction: scrollDirection,
@@ -6607,6 +6608,8 @@ class Selection {
   _isActive = true;
   _isDragging = true;
   _isStart = false;
+  _mode = "character";
+  _hasExpanded = false;
   constructor(anchorRenderable, anchor, focus) {
     this._anchor = new SelectionAnchor(anchorRenderable, anchor.x, anchor.y);
     this._focus = { ...focus };
@@ -6638,6 +6641,18 @@ class Selection {
   set isDragging(value) {
     this._isDragging = value;
   }
+  get mode() {
+    return this._mode;
+  }
+  set mode(value) {
+    this._mode = value;
+  }
+  get hasExpanded() {
+    return this._hasExpanded;
+  }
+  set hasExpanded(value) {
+    this._hasExpanded = value;
+  }
   get bounds() {
     const minX = Math.min(this._anchor.x, this._focus.x);
     const maxX = Math.max(this._anchor.x, this._focus.x);
@@ -6689,6 +6704,118 @@ function convertGlobalToLocalSelection(globalSelection, localX, localY) {
     isActive: true
   };
 }
+function clamp(value, min, max) {
+  return Math.max(min, Math.min(max, value));
+}
+function wordClass(char) {
+  if (!char)
+    return "space";
+  if (/\s/u.test(char))
+    return "space";
+  return "word";
+}
+function expandWordBounds(text, column) {
+  if (!text.length) {
+    return { start: 0, end: 0 };
+  }
+  const safeColumn = clamp(column, 0, text.length);
+  const index = safeColumn === text.length && wordClass(text[text.length - 1]) !== "space" ? Math.max(0, text.length - 1) : safeColumn;
+  const kind = wordClass(text[index]);
+  let start = index;
+  let end = Math.min(text.length, index + 1);
+  while (start > 0 && wordClass(text[start - 1]) === kind) {
+    start--;
+  }
+  while (end < text.length && wordClass(text[end]) === kind) {
+    end++;
+  }
+  return { start, end };
+}
+function lineTextAt(text, line) {
+  let start = 0;
+  for (let idx = 0;idx < line; idx++) {
+    const next = text.indexOf(`\n`, start);
+    if (next === -1)
+      return "";
+    start = next + 1;
+  }
+  const end = text.indexOf(`\n`, start);
+  return end === -1 ? text.slice(start) : text.slice(start, end);
+}
+function lineInfoAt(renderable, localY) {
+  const lineInfo = renderable.lineInfo;
+  const visualLine = clamp(Math.floor(renderable.scrollY + localY), 0, Math.max(0, lineInfo.lineSources.length - 1));
+  const sourceLine = lineInfo.lineSources[visualLine] ?? visualLine;
+  const startCol = lineInfo.lineStartCols[visualLine] ?? 0;
+  const text = lineTextAt(renderable.plainText, sourceLine);
+  return {
+    text,
+    startCol,
+    sourceLine,
+    visualLine,
+  };
+}
+function paragraphEdgePoint(renderable, info, edge) {
+  const lineInfo = renderable.lineInfo;
+  let firstVisualLine = info.visualLine;
+  let lastVisualLine = info.visualLine;
+  for (let idx = info.visualLine;idx >= 0; idx--) {
+    if ((lineInfo.lineSources[idx] ?? idx) !== info.sourceLine) {
+      break;
+    }
+    firstVisualLine = idx;
+  }
+  for (let idx = info.visualLine;idx < lineInfo.lineSources.length; idx++) {
+    if ((lineInfo.lineSources[idx] ?? idx) !== info.sourceLine) {
+      break;
+    }
+    lastVisualLine = idx;
+  }
+  const visualLine = edge === "start" ? firstVisualLine : lastVisualLine;
+  const startCol = lineInfo.lineStartCols[visualLine] ?? 0;
+  return {
+    x: edge === "start" ? 0 : Math.max(0, info.text.length - startCol),
+    y: visualLine - renderable.scrollY,
+  };
+}
+function expandSelectionPoint(renderable, localX, localY, edge, mode) {
+  if (mode === "character") {
+    return { x: localX, y: localY };
+  }
+  if (localY < 0 || localY >= renderable.height) {
+    return { x: localX, y: localY };
+  }
+  const info = lineInfoAt(renderable, localY);
+  if (mode === "paragraph") {
+    return paragraphEdgePoint(renderable, info, edge);
+  }
+  const column = clamp(info.startCol + Math.max(0, localX), 0, info.text.length);
+  const bounds = expandWordBounds(info.text, column);
+  return {
+    x: (edge === "start" ? bounds.start : bounds.end) - info.startCol,
+    y: localY,
+  };
+}
+function adjustLocalSelection(renderable, localSelection, mode) {
+  if (!localSelection?.isActive || mode === "character") {
+    return localSelection;
+  }
+  const anchor = expandSelectionPoint(renderable, localSelection.anchorX, localSelection.anchorY, localSelection.anchorY < localSelection.focusY || localSelection.anchorY === localSelection.focusY && localSelection.anchorX <= localSelection.focusX ? "start" : "end", mode);
+  const focus = expandSelectionPoint(renderable, localSelection.focusX, localSelection.focusY, localSelection.anchorY < localSelection.focusY || localSelection.anchorY === localSelection.focusY && localSelection.anchorX <= localSelection.focusX ? "end" : "start", mode);
+  return {
+    ...localSelection,
+    anchorX: anchor.x,
+    anchorY: anchor.y,
+    focusX: focus.x,
+    focusY: focus.y,
+  };
+}
+function selectionBgFor(renderable) {
+  return renderable._selectionBg ?? renderable._ctx?.selectionBg;
+}
+function selectionFgFor(renderable) {
+  return renderable._selectionFg ?? renderable._ctx?.selectionFg;
+}
 
 class ASCIIFontSelectionHelper {
   getText;
@@ -18110,16 +18237,19 @@ class TextBufferRenderable extends Renderable {
     return localX >= 0 && localX < this.width && localY >= 0 && localY < this.height;
   }
   onSelectionChanged(selection2) {
-    const localSelection = convertGlobalToLocalSelection(selection2, this.x, this.y);
+    const mode = selection2?.mode ?? "character";
+    const localSelection = adjustLocalSelection(this, convertGlobalToLocalSelection(selection2, this.x, this.y), mode);
     this.lastLocalSelection = localSelection;
     let changed;
     if (!localSelection?.isActive) {
+      this.textBufferView.resetSelection();
       this.textBufferView.resetLocalSelection();
       changed = true;
     } else if (selection2?.isStart) {
-      changed = this.textBufferView.setLocalSelection(localSelection.anchorX, localSelection.anchorY, localSelection.focusX, localSelection.focusY, this._selectionBg, this._selectionFg);
+      this.textBufferView.resetSelection();
+      changed = this.textBufferView.setLocalSelection(localSelection.anchorX, localSelection.anchorY, localSelection.focusX, localSelection.focusY, selectionBgFor(this), selectionFgFor(this));
     } else {
-      changed = this.textBufferView.updateLocalSelection(localSelection.anchorX, localSelection.anchorY, localSelection.focusX, localSelection.focusY, this._selectionBg, this._selectionFg);
+      changed = this.textBufferView.updateLocalSelection(localSelection.anchorX, localSelection.anchorY, localSelection.focusX, localSelection.focusY, selectionBgFor(this), selectionFgFor(this));
     }
     if (changed) {
       this.requestRender();
@@ -20275,7 +20405,7 @@ class EditBufferRenderable extends Renderable {
       this.editorView.resetLocalSelection();
       return true;
     }
-    return this.editorView.setLocalSelection(localSelection.anchorX, localSelection.anchorY, localSelection.focusX, localSelection.focusY, this._selectionBg, this._selectionFg, false);
+    return this.editorView.setLocalSelection(localSelection.anchorX, localSelection.anchorY, localSelection.focusX, localSelection.focusY, selectionBgFor(this), selectionFgFor(this), false);
   }
   shouldStartSelection(x, y) {
     if (!this.selectable)
@@ -20285,19 +20415,22 @@ class EditBufferRenderable extends Renderable {
     return localX >= 0 && localX < this.width && localY >= 0 && localY < this.height;
   }
   onSelectionChanged(selection2) {
-    const localSelection = convertGlobalToLocalSelection(selection2, this.x, this.y);
+    const mode = selection2?.mode ?? "character";
+    const localSelection = adjustLocalSelection(this, convertGlobalToLocalSelection(selection2, this.x, this.y), mode);
     this.lastLocalSelection = localSelection;
     const updateCursor = true;
     const followCursor = this._keyboardSelectionActive;
     let changed;
     if (!localSelection?.isActive) {
       this._keyboardSelectionActive = false;
+      this.editorView.resetSelection();
       this.editorView.resetLocalSelection();
       changed = true;
     } else if (selection2?.isStart) {
-      changed = this.editorView.setLocalSelection(localSelection.anchorX, localSelection.anchorY, localSelection.focusX, localSelection.focusY, this._selectionBg, this._selectionFg, updateCursor, followCursor);
+      this.editorView.resetSelection();
+      changed = this.editorView.setLocalSelection(localSelection.anchorX, localSelection.anchorY, localSelection.focusX, localSelection.focusY, selectionBgFor(this), selectionFgFor(this), updateCursor, followCursor);
     } else {
-      changed = this.editorView.updateLocalSelection(localSelection.anchorX, localSelection.anchorY, localSelection.focusX, localSelection.focusY, this._selectionBg, this._selectionFg, updateCursor, followCursor);
+      changed = this.editorView.updateLocalSelection(localSelection.anchorX, localSelection.anchorY, localSelection.focusX, localSelection.focusY, selectionBgFor(this), selectionFgFor(this), updateCursor, followCursor);
     }
     if (changed && localSelection?.isActive && selection2?.isDragging) {
       const viewport = this.editorView.getViewport();
@@ -20357,7 +20490,7 @@ class EditBufferRenderable extends Renderable {
     const selection2 = this.getSelection();
     if (!selection2)
       return;
-    this.editorView.setSelection(selection2.start, selection2.end, this._selectionBg, this._selectionFg);
+    this.editorView.setSelection(selection2.start, selection2.end, selectionBgFor(this), selectionFgFor(this));
   }
   deleteSelectedText() {
     this.editorView.deleteSelectedText();
@@ -20368,7 +20501,7 @@ class EditBufferRenderable extends Renderable {
     this.lastLocalSelection = null;
     this.editorView.resetLocalSelection();
     this._ctx.clearSelection();
-    this.editorView.setSelection(start, end, this._selectionBg, this._selectionFg);
+    this.editorView.setSelection(start, end, selectionBgFor(this), selectionFgFor(this));
     this.requestRender();
   }
   setSelectionInclusive(start, end) {
@@ -21541,6 +21674,8 @@ class CliRenderer extends EventEmitter9 {
   maxStatSamples = 300;
   postProcessFns = [];
   backgroundColor = RGBA.fromInts(0, 0, 0, 0);
+  selectionBg = undefined;
+  selectionFg = undefined;
   waitingForPixelResolution = false;
   clock;
   rendering = false;
@@ -23016,6 +23151,8 @@ Captured external output:
   focusHandler = ((sequence) => {
     if (sequence === "\x1B[I") {
       if (this.shouldRestoreModesOnNextFocus) {
+        this._lastSelectionClick = undefined; this.setCapturedRenderable(undefined);
+        this.stdinParser?.resetMouseState();
         this.lib.restoreTerminalModes(this.rendererPtr);
         this.shouldRestoreModesOnNextFocus = false;
       }
@@ -23029,6 +23166,8 @@ Captured external output:
       this.shouldRestoreModesOnNextFocus = true;
       if (this._terminalFocusState !== false) {
         this._terminalFocusState = false;
+        this._lastSelectionClick = undefined; this.setCapturedRenderable(undefined);
+        this.stdinParser?.resetMouseState();
         this.emit("blur" /* BLUR */);
       }
       return true;
@@ -23166,6 +23305,9 @@ Captured external output:
       const scrollTarget = maybeRenderable2 ?? fallbackTarget;
       if (scrollTarget) {
         const event2 = new MouseEvent(scrollTarget, mouseEvent);
+        this._lastSelectionClick = undefined; this.setCapturedRenderable(undefined);
+        if (this.currentSelection?.isDragging) this.clearSelection();
+        this.stdinParser?.resetMouseState();
         scrollTarget.processMouseEvent(event2);
       }
       return true;
@@ -23177,7 +23319,7 @@ Captured external output:
     if (mouseEvent.type === "down" && mouseEvent.button === 0 /* LEFT */ && !this.currentSelection?.isDragging && !mouseEvent.modifiers.ctrl) {
       const canStartSelection = Boolean(maybeRenderable && maybeRenderable.selectable && !maybeRenderable.isDestroyed && maybeRenderable.shouldStartSelection(mouseEvent.x, mouseEvent.y));
       if (canStartSelection && maybeRenderable) {
-        this.startSelection(maybeRenderable, mouseEvent.x, mouseEvent.y);
+        this.startSelection(maybeRenderable, mouseEvent.x, mouseEvent.y, this.getSelectionClickMode(maybeRenderable, mouseEvent.x, mouseEvent.y));
         this.dispatchMouseEvent(maybeRenderable, mouseEvent);
         return true;
       }
@@ -23421,6 +23563,18 @@ Captured external output:
     this.nextRenderBuffer.clear(parsedColor);
     this.requestRender();
   }
+  setSelectionColors(selectionBg, selectionFg) {
+    const parsedBg = selectionBg ? parseColor(selectionBg) : undefined;
+    const parsedFg = selectionFg ? parseColor(selectionFg) : undefined;
+    if (this.selectionBg?.r === parsedBg?.r && this.selectionBg?.g === parsedBg?.g && this.selectionBg?.b === parsedBg?.b && this.selectionBg?.a === parsedBg?.a && this.selectionFg?.r === parsedFg?.r && this.selectionFg?.g === parsedFg?.g && this.selectionFg?.b === parsedFg?.b && this.selectionFg?.a === parsedFg?.a)
+      return;
+    this.selectionBg = parsedBg;
+    this.selectionFg = parsedFg;
+    if (this.currentSelection) {
+      this.notifySelectablesOfSelectionChange();
+    }
+    this.requestRender();
+  }
   toggleDebugOverlay() {
     const willBeEnabled = !this.debugOverlay.enabled;
     if (willBeEnabled && !this.memorySnapshotInterval) {
@@ -23899,6 +24053,18 @@ Captured external output:
   getSelectionContainer() {
     return this.selectionContainers.length > 0 ? this.selectionContainers[this.selectionContainers.length - 1] : null;
   }
+  getSelectionClickMode(renderable, x, y) {
+    const now = Date.now();
+    const previous = this._lastSelectionClick;
+    const sameTarget = previous && previous.renderable === renderable && Math.abs(previous.x - x) <= 1 && Math.abs(previous.y - y) <= 1;
+    const count = sameTarget && now - previous.time <= 500 ? Math.min(previous.count + 1, 3) : 1;
+    this._lastSelectionClick = { renderable, x, y, time: now, count };
+    if (count === 2)
+      return "word";
+    if (count === 3)
+      return "paragraph";
+    return "character";
+  }
   clearSelection() {
     if (this.currentSelection) {
       for (const renderable of this.currentSelection.touchedRenderables) {
@@ -23910,19 +24076,21 @@ Captured external output:
     }
     this.selectionContainers = [];
   }
-  startSelection(renderable, x, y) {
+  startSelection(renderable, x, y, mode = "character") {
     if (!renderable.selectable)
       return;
     this.clearSelection();
     this.selectionContainers.push(renderable.parent || this.root);
     this.currentSelection = new Selection(renderable, { x, y }, { x, y });
     this.currentSelection.isStart = true;
+    this.currentSelection.mode = mode;
     this.notifySelectablesOfSelectionChange();
   }
   updateSelection(currentRenderable, x, y, options) {
     if (this.currentSelection) {
       this.currentSelection.isStart = false;
       this.currentSelection.focus = { x, y };
+      this.currentSelection.hasExpanded = this.currentSelection.hasExpanded || Math.abs(this.currentSelection.anchor.x - x) > 1 || Math.abs(this.currentSelection.anchor.y - y) > 1;
       if (options?.finishDragging) {
         this.currentSelection.isDragging = false;
       }
@@ -23965,6 +24133,9 @@ Captured external output:
    finishSelection() {
      if (this.currentSelection) {
        this.currentSelection.isDragging = false;
+      if (this.currentSelection.hasExpanded) {
+        this._lastSelectionClick = undefined;
+      }
        this.emit("selection" /* SELECTION */, this.currentSelection);
        this.notifySelectablesOfSelectionChange();
      }
@@ -20638,6 +20771,24 @@ class EditBufferRenderable extends Renderable {
      return true;
    }
    const currentCursor = this.editBuffer.getCursorPosition();
+    const text = this.editBuffer.getText();
+    let targetOffset = currentCursor.offset;
+    while (targetOffset > 0 && /\s/.test(text[targetOffset - 1] ?? "")) {
+      targetOffset--;
+    }
+    const virtualExtmark = this.editorView.extmarks.getAll().find((extmark) => extmark.virtual && targetOffset >= extmark.start && targetOffset <= extmark.end);
+    if (virtualExtmark) {
+      this.cursorOffset = virtualExtmark.start;
+      const startCursor = this.logicalCursor;
+      this.cursorOffset = currentCursor.offset;
+      const endCursor = this.logicalCursor;
+      this.editorView.extmarks.delete(virtualExtmark.id);
+      this.editBuffer.deleteRange(startCursor.row, startCursor.col, endCursor.row, endCursor.col);
+      this.cursorOffset = virtualExtmark.start;
+      this._ctx.clearSelection();
+      this.requestRender();
+      return true;
+    }
    const prevWord = this.editBuffer.getPrevWordBoundary();
    if (prevWord.offset < currentCursor.offset) {
      this.editBuffer.deleteRange(prevWord.row, prevWord.col, currentCursor.row, currentCursor.col);
diff --git a/renderables/Markdown.d.ts b/renderables/Markdown.d.ts
index bd0da2783..e5d262b62 100644
--- a/renderables/Markdown.d.ts
+++ b/renderables/Markdown.d.ts
@@ -56,6 +56,14 @@ export interface MarkdownTableOptions {
      * Enables/disables selection support on markdown tables.
      */
     selectable?: boolean;
+    /**
+     * Selection background for markdown tables.
+     */
+    selectionBg?: ColorInput;
+    /**
+     * Selection foreground for markdown tables.
+     */
+    selectionFg?: ColorInput;
 }
 export interface MarkdownOptions extends RenderableOptions<MarkdownRenderable> {
     content?: string;
diff --git a/renderables/TextTable.d.ts b/renderables/TextTable.d.ts
index 956071861..ae92570a4 100644
--- a/renderables/TextTable.d.ts
+++ b/renderables/TextTable.d.ts
@@ -85,6 +85,10 @@ export declare class TextTableRenderable extends Renderable {
     set borderStyle(value: BorderStyle);
     get borderColor(): RGBA;
     set borderColor(value: ColorInput);
+    get selectionBg(): RGBA | undefined;
+    set selectionBg(value: ColorInput | undefined);
+    get selectionFg(): RGBA | undefined;
+    set selectionFg(value: ColorInput | undefined);
     shouldStartSelection(x: number, y: number): boolean;
     onSelectionChanged(selection: Selection | null): boolean;
     hasSelection(): boolean;
diff --git a/renderer.d.ts b/renderer.d.ts
index a3bc58f08..d0f7c851e 100644
--- a/renderer.d.ts
+++ b/renderer.d.ts
@@ -196,6 +196,8 @@ export declare class CliRenderer extends EventEmitter implements RenderContext {
     private maxStatSamples;
     private postProcessFns;
     private backgroundColor;
+    selectionBg: RGBA | undefined;
+    selectionFg: RGBA | undefined;
     private waitingForPixelResolution;
     private readonly clock;
     private rendering;
@@ -412,6 +414,7 @@ export declare class CliRenderer extends EventEmitter implements RenderContext {
     private queryPixelResolution;
     private processResize;
     setBackgroundColor(color: ColorInput): void;
+    setSelectionColors(selectionBg: ColorInput | undefined, selectionFg: ColorInput | undefined): void;
     toggleDebugOverlay(): void;
     configureDebugOverlay(options: {
         enabled?: boolean;
diff --git a/types.d.ts b/types.d.ts
index baab4070f..ff0b339ea 100644
--- a/types.d.ts
+++ b/types.d.ts
@@ -67,6 +67,8 @@ export interface RenderContext extends EventEmitter {
     /** Monotonic, bumped once per `loop()` iteration. Lets renderables dedupe per-frame work. */
     frameId: number;
     requestRender: () => void;
+    selectionBg: RGBA | undefined;
+    selectionFg: RGBA | undefined;
     setCursorPosition: (x: number, y: number, visible: boolean) => void;
     setCursorStyle: (options: CursorStyleOptions) => void;
     setCursorColor: (color: RGBA) => void;
